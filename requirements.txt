torch>=1.9.0
torchvision>=0.10.0
scikit-learn>=0.24.0
tqdm>=4.62.0
# tensorboard  # 已包含在torch中（torch.utils.tensorboard）
# 可选：如果需要GPU支持，请安装对应CUDA版本的PyTorch
# 访问 https://pytorch.org/get-started/locally/ 获取详细安装命令